name: Validate

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * 0" # weekly, catches breakage from new HA releases

jobs:
  hassfest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: home-assistant/actions/hassfest@master

  hacs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hacs/action@main
        with:
          category: integration
